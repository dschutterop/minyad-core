# Aggregate of every service's own requirements.txt, for local dev/test where
# all services run in one virtualenv (PYTHONPATH=. pytest). Each Docker image
# installs only its own <service>/requirements.txt -- see that file for what
# a given service actually needs.
-r api/requirements.txt
-r control/requirements.txt
-r ingestion/requirements.txt
-r deadman/requirements.txt
-r forecast/requirements.txt
-r reporting/requirements.txt
-r frontend/requirements.txt
-r mobile-frontend/requirements.txt
-r monitoring/requirements.txt
-r migrate/requirements.txt
-r minyad-strategy/requirements.txt

# Only used by scripts/dump_agent_decisions.py, a local/manual maintenance
# script -- not part of any service image.
psycopg[binary]==3.3.4
