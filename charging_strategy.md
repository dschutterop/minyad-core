# Battery Charge/Discharge Strategy — superseded

This document described the v1/v2 battery strategy design (the reactive `HysteresisController`
FSM, the dormant `ChargeController`, and the rebuild prompt that produced `minyad/strategy/v2/`).

It has been superseded by [`strategy_v3.md`](strategy_v3.md), the authoritative spec for the
predictive LP-planner-based strategy in `minyad/strategy/v3/`. Consult git history for this
file's prior content if you need the v1/v2 design record.
