[project]
name = "minyad-core"
version = "0.1.0"
description = "Home energy plumbing: battery/PV/grid-meter/MQTT/Prometheus integration."
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
preview = true
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF", "fast-api-non-annotated-dependency"]
ignore = [
    "line-too-long", # dominated by inline HTML/CSS/JS strings in frontend/*, not a real readability issue
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = [
    "float-equality-comparison", # assertions against deterministically-computed fixtures are the intended test pattern
]
"frontend/*.py" = [
    "ambiguous-unicode-character-string", # deliberate typography (en-dash, multiplication sign, angle quotes) in user-facing UI copy
]
"api/main.py" = [
    "unused-async", # ruff's FastAPI-route exemption only fires when `app = FastAPI()` is local to the
              # file; api/state.py owns that now, so route handlers that don't happen to await
              # anything get flagged even though `async def` is the correct, consistent signature
              # for a route handler.
]
"api/routers/*.py" = [
    "unused-async", # same as api/main.py above, but for `router = APIRouter()` instances.
]

[tool.pyright]
include = ["api", "control", "host-services", "ingestion", "forecast", "reporting", "shared", "scripts", "migrate"]
pythonVersion = "3.12"
typeCheckingMode = "basic"
reportMissingImports = "warning" # tolerates the optional, privately-hosted minyad.strategy.v3 import
reportPrivateImportUsage = "none" # paho-mqtt exports CallbackAPIVersion/ReasonCode/Properties at runtime but omits them from __all__
