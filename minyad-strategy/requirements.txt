fastapi==0.138.0
starlette==1.3.1
uvicorn[standard]==0.49.0
sqlalchemy==2.0.51
asyncpg==0.31.0
cryptography==49.0.0
paho-mqtt==2.1.0
httpx==0.28.1
apscheduler==3.11.1
# pulp/coinor-cbc are unused by this repo's public v2 service, but this
# Dockerfile also builds the private minyad-strategy-v3 service (see
# docker-compose.private.yml in minyad-deploy), whose LP planner needs them.
pulp==2.9.0
